<p id="notice"><%= notice %></p>

<h1>M Icroposts</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @m_icroposts.each do |m_icropost| %>
      <tr>
        <td><%= m_icropost.content %></td>
        <td><%= m_icropost.user_id %></td>
        <td><%= link_to 'Show', m_icropost %></td>
        <td><%= link_to 'Edit', edit_m_icropost_path(m_icropost) %></td>
        <td><%= link_to 'Destroy', m_icropost, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New M Icropost', new_m_icropost_path %>
